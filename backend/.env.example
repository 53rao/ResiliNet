# Comma-separated exact browser origins. Do not use *.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Reserved by the M0 contract for later milestones.
GRAPH_PATH=data/graph.json
REPORT_MODE=template
OPTIMIZER_MAX_SECONDS=8
OPTIMIZER_MAX_ITERATIONS=300

# Optional in M6 only. Keep credentials server-side.
GROQ_API_KEY=
GROQ_MODEL=
